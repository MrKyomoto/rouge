[package]
name = "rogue"
version = "0.1.0"
edition = "2024"

[dependencies]
bracket-lib = "0.8.7"
serde = { version = "1.0.228", features = ["derive"] }
specs = {version = "0.20.0", features = ["serde"] }
specs-derive = "0.4.1"
